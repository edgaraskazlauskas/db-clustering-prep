// @ts-check

const Metric = require("./Metric");

/**
 * This metric evaluates whether the domain roots (tables with the most relations) were split out.
 */
module.exports = class DomainRootSeparationMetric extends Metric {
  async evaluate() {
    // Missing impl
  }
};
